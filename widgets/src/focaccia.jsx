// The focaccia widget's source of truth is the standalone foccaciabot repo,
// installed as the `focaccia-widget` git dependency (github:4AM365/foccaciabot).
// Edit + push there, then `npm update focaccia-widget && npm run build:widgets`.
export { default } from "focaccia-widget"
